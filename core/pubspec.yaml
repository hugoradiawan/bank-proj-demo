name: core
description: "A new Flutter package project."
version: 0.0.1

resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter
  build_runner: ^2.4.15
  freezed: ^3.0.0
  json_serializable: ^6.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

